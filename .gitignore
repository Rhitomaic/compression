# Old Python prototype — preserved on the `old` branch, not in main.
compression/

# .NET build artifacts
bin/
obj/
*.user
*.suo
*.userprefs
*.cache
project.lock.json
artifacts/

# Publish output (we attach the zip to GitHub releases, not commit it)
publish/
*.zip
*.7z

# IDE
.vs/
.vscode/
.idea/
*.swp
*.swo

# Claude Code local settings
.claude/

# Runtime data — downloaded or generated by SmartCompress on first run.
# These live next to the exe in portable mode; users get their own.
ffmpeg/
out/
cqp_cache.json
benchmark_cache.json
predictor_calibration.json

# Logs
*.log

# OS noise
Thumbs.db
.DS_Store
desktop.ini
